from typing import List


def list_sum(list: List[int]) -> int:
    """Make sum of list

    Args:
        list (List[int]): list with integers

    Returns:
        int: sum of list
    """    
    return sum(list)
